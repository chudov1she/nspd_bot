# Telegram Bot для работы с Росреестром

Telegram бот для получения данных об объектах недвижимости из Росреестра по кадастровым номерам с автоматической генерацией карт для земельных участков.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
# Создайте виртуальное окружение (если еще не создано)
python -m venv venv

# Активируйте виртуальное окружение
# Windows:
.\venv\Scripts\Activate.ps1
# Linux/Mac:
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Установите браузеры для Playwright (для генерации карт)
playwright install chromium
```

### 2. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
# Обязательно
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Администраторы (через запятую или пробел)
ADMIN_IDS=123456789,2064202147

# API Росреестра (опционально, можно использовать режим симуляции)
API_ROSREESTR_KEY=your_api_key_here
API_SIMULATION_MODE=true  # true для тестирования без реального API ключа

# Опционально
LOG_LEVEL=INFO
API_ROSREESTR_TIMEOUT=120
```

Токен бота можно получить у [@BotFather](https://t.me/BotFather) в Telegram.

### 3. Настройка администраторов

Добавьте ID администраторов в `.env`:

```env
# ID администраторов (через запятую или пробел)
ADMIN_IDS=123456789,987654321
```

**Как узнать свой Telegram ID:**
- Напишите боту [@userinfobot](https://t.me/userinfobot) в Telegram
- Или используйте [@getidsbot](https://t.me/getidsbot)

### 4. Запуск бота

**На локальном компьютере (Windows/Mac/Linux с GUI):**
```bash
# Запустите бота
python run.py
```

**На Linux сервере без GUI (Ubuntu 22.04 и другие):**

⚠️ **Важно:** Браузер работает в режиме `headless=False` (требует графический интерфейс). Для Linux серверов используйте Xvfb.

1. Установите Xvfb (виртуальный дисплей):
```bash
sudo apt-get update
sudo apt-get install -y xvfb
```

2. Запустите бота через xvfb-run:
```bash
xvfb-run -a python run.py
```

**Или настройте автозапуск через systemd** (см. раздел "Установка на Linux сервер" ниже).

При первом запуске администраторы из `.env` будут автоматически добавлены в базу данных.

**Альтернативные способы управления администраторами:**
- Через скрипт: `python scripts/add_admin.py <telegram_id>`
- Через команды бота (если уже есть админ):
  - `/admin_list` - список администраторов
  - `/admin_add <telegram_id>` - добавить администратора
  - `/admin_remove <telegram_id>` - удалить администратора

## 📋 Использование

### Основные функции

1. **Росреестр выгрузка**
   - Ввод кадастровых номеров текстом (один или несколько через запятую)
   - Загрузка Excel файла с кадастровыми номерами (номера ищутся во всех ячейках)
   - Получение данных по объектам недвижимости
   - Автоматическая генерация карт для земельных участков
   - Формирование отчетов в Excel

2. **Задачи**
   - Просмотр всех завершенных задач (успешных и с ошибками)
   - Детальная информация по каждой задаче
   - Статистика обработки и баланс API
   - Пагинация для большого количества задач

### Формат кадастрового номера

```
XX:XX:XXXXXXX:XXXXXXX
```

Где:
- Первые две части: 2 цифры каждая (XX:XX)
- Третья часть: минимум 6 цифр (XXXXXXX)
- Четвертая часть: от 1 до 7 цифр (X до XXXXXXX)

**Примеры:**
- `78:38:0022629:1115` (4 цифры в последней части)
- `78:42:0018329:12213` (5 цифр в последней части)
- `22:61:020713:45` (2 цифры в последней части)
- `78:38:0022629:1115, 78:38:0022629:1006` (несколько номеров через запятую)

## ⚙️ Принцип работы приложения

### Архитектура системы

Приложение построено на асинхронной архитектуре с использованием очереди задач и фонового воркера для последовательной обработки запросов.

### Процесс обработки задачи

#### 1. Создание задачи

**Ввод текста:**
- Пользователь отправляет текст с кадастровыми номерами
- Бот извлекает все номера через регулярные выражения
- Создается задача в БД со статусом `PENDING`
- Задача добавляется в очередь обработки

**Загрузка файла:**
- Пользователь загружает Excel файл (XLS/XLSX)
- Файл сохраняется во временную директорию
- Бот сканирует все ячейки всех листов файла
- Извлекаются все найденные кадастровые номера
- Создается задача в БД со статусом `PENDING`
- Задача добавляется в очередь обработки

#### 2. Очередь задач (FIFO)

- Все задачи хранятся в базе данных SQLite
- Очередь работает по принципу **FIFO** (First In, First Out)
- Задачи обрабатываются строго последовательно, одна за другой
- При добавлении задачи пользователь видит её позицию в очереди
- Можно добавить несколько задач подряд - они будут обработаны по порядку

#### 3. Фоновый воркер

**Основной цикл:**
- Воркер запускается при старте бота и работает в фоновом режиме
- Постоянно проверяет очередь на наличие задач со статусом `PENDING`
- Берет самую старую задачу (по `created_at`)
- Меняет статус задачи на `PROCESSING`
- Обрабатывает задачу полностью
- Только после завершения берет следующую задачу

**Обработка одной задачи:**

1. **Парсинг кадастровых номеров**
   - Извлекает номера из текста или файла
   - Валидирует формат каждого номера
   - Удаляет дубликаты

2. **Проверка API**
   - Проверяет наличие и доступность API ключа
   - Проверяет доступность сервиса Росреестра

3. **Получение данных через API**
   - Для каждого кадастрового номера отправляется запрос к API
   - Получаются данные об объекте недвижимости:
     - Наименование, адрес, площадь
     - Категория, разрешенное использование
     - Кадастровая стоимость
     - Правообладатель, обременения
     - Координаты (для земельных участков)
   - Если API возвращает `found: false`, система автоматически пытается получить данные с карты nspd.gov.ru
   - Обрабатываются ошибки для каждого номера отдельно

4. **Генерация карт для земельных участков**
   - Определяются объекты типа "земельный участок"
   - Для каждого участка создается задача генерации карты в БД
   - Проверка на дубликаты (если карта уже есть - используется существующая)
   - Генерация карт через Playwright:
     - Открытие сайта nspd.gov.ru/map
     - Поиск кадастрового номера (увеличенные таймауты для медленной загрузки)
     - Ожидание результатов поиска (до 45 секунд)
     - Клик по найденному объекту (увеличенные таймауты)
     - Ожидание загрузки карты (до 45 секунд)
     - Уменьшение масштаба карты
     - Снимок экрана и обрезка UI элементов
   - Сохранение карт в отдельные файлы
   - Если карта не найдена или произошла ошибка - в отчёте отображается соответствующий текст

5. **Формирование Excel отчёта**
   - Создается один файл с данными и картами:
     - 18 колонок с характеристиками объектов:
       - №, Наименование, Кадастровый номер
       - Адрес, Площадь, Категория, Разрешенное использование
       - Кадастровая стоимость, Передаваемые права
       - Правообладатель, Обременения, Этаж
       - Статус объекта, Назначение
       - Дата регистрации права, Дата обновления информации
       - Старый кадастровый номер, Дата кадастровой стоимости
       - **Карта** (только для земельных участков)
     - Форматирование чисел по российским стандартам (запятая для десятичных, без разделителя тысяч)
     - Выделение ошибок красным цветом
     - Карты земельных участков вставляются в колонку "Карта":
       - Изображения центрированы в ячейке
       - Если карта не найдена - отображается "Карта не найдена"
       - Если ошибка загрузки - отображается "Ошибка загрузки карты"
     - При загрузке исходного файла - данные добавляются в новые колонки

6. **Отправка результатов**
   - Отправляется текстовое сообщение со статистикой
   - Отправляется Excel файл с данными и картами
   - Обновляется статус задачи на `COMPLETED` или `FAILED`
   - Сохраняется баланс API после обработки

#### 4. Управление задачами генерации карт

- Отдельная система задач для генерации карт в БД
- Защита от дубликатов - проверка существующих карт
- Система повторных попыток (до 3 раз при ошибках)
- Использование существующих карт вместо повторной генерации

#### 5. Уведомления пользователя

- При создании задачи: номер задачи, количество номеров, позиция в очереди
- При начале обработки: уведомление о начале работы
- При завершении: детальная статистика, баланс API, информация о картах

### Особенности реализации

**Асинхронность:**
- Все операции выполняются асинхронно (asyncio)
- Неблокирующая обработка задач
- Параллельная работа бота и воркера

**Надежность:**
- Все задачи сохраняются в БД
- История всех операций
- Обработка ошибок на каждом этапе
- Graceful shutdown при остановке бота

**Производительность:**
- Последовательная обработка предотвращает перегрузку API
- Кэширование существующих карт
- Оптимизированная работа с Excel файлами

**Масштабируемость:**
- Легко добавить несколько воркеров (при необходимости)
- Модульная архитектура
- Разделение ответственности между компонентами

## 📁 Структура проекта

```
telegram_app/
├── bot/                    # Основной код бота
│   ├── config/             # Настройки
│   ├── database/           # Модели БД
│   ├── handlers/           # Обработчики сообщений
│   ├── keyboards/           # Клавиатуры
│   ├── models/             # Модели данных
│   ├── services/           # Сервисы (API, парсер, Excel, генератор карт)
│   ├── states/             # FSM состояния
│   └── utils/              # Утилиты
├── data/                   # Данные
│   ├── input/              # Входные файлы
│   └── output/             # Выходные файлы
├── scripts/                # Вспомогательные скрипты
├── logs/                   # Логи
├── .env                    # Переменные окружения (не коммитится)
├── requirements.txt        # Зависимости
└── run.py                  # Точка входа
```

## 🛠 Технологии

- **Python 3.10+**
- **aiogram 3.23.0** - Telegram Bot API
- **SQLAlchemy 2.0.36** - ORM для работы с БД
- **aiosqlite 0.20.0** - Асинхронный SQLite
- **openpyxl 3.1.2** - Работа с Excel
- **aiohttp 3.9.0** - HTTP клиент
- **playwright 1.40.0** - Генерация карт
- **loguru 0.7.2** - Логирование
- **pydantic 2.12.5** - Валидация данных
- **pytz 2024.1** - Работа с часовыми поясами

## ✨ Особенности

- ✅ Модульная архитектура
- ✅ Асинхронная обработка задач через очередь (FIFO)
- ✅ Последовательная обработка нескольких задач
- ✅ Автоматическая генерация карт для земельных участков
- ✅ Поддержка режима симуляции API для тестирования
- ✅ Система управления пользователями через БД
- ✅ Детальное логирование всех операций
- ✅ Обработка ошибок и уведомления пользователей
- ✅ История всех задач с пагинацией (успешные и с ошибками)
- ✅ Московское время для всех дат и файлов
- ✅ Защита от дубликатов при генерации карт
- ✅ Система повторных попыток при ошибках
- ✅ Автоматическое получение данных с карты при отсутствии в API
- ✅ Карты земельных участков в основном отчёте
- ✅ Увеличенные таймауты для надёжной загрузки карт
- ✅ Информативные сообщения при отсутствии карт

## 📝 Команды бота

- `/start` - Главное меню
- `/menu` - Главное меню
- `/admin_list` - Список администраторов (только для админов)
- `/admin_add <id>` - Добавить администратора (только для админов)
- `/admin_remove <id>` - Удалить администратора (только для админов)

## 🔒 Доступ

**Доступ к боту имеют только администраторы.**

Для получения доступа пользователь должен быть добавлен в список администраторов:
- Через `.env` файл (при запуске бота)
- Через команду `/admin_add <telegram_id>` (существующим администратором)
- Через скрипт `python scripts/add_admin.py <telegram_id>`

## 🖥️ Установка на Linux сервер (Ubuntu 22.04)

### Рекомендуемая конфигурация
- **ОС:** Ubuntu 22.04 LTS
- **RAM:** 2 ГБ (минимум)
- **HDD:** 20-30 ГБ
- **vCPU:** 1-2 ядра

### Пошаговая установка

#### 1. Подключение к серверу
```bash
ssh user@your-server-ip
```

#### 2. Обновление системы
```bash
sudo apt-get update
sudo apt-get upgrade -y
```

#### 3. Установка Python и зависимостей
```bash
sudo apt-get install -y python3.10 python3.10-venv python3-pip git
```

#### 4. Установка Xvfb и зависимостей для Playwright
```bash
# Xvfb (виртуальный дисплей)
sudo apt-get install -y xvfb

# Зависимости для Playwright/Chrome
sudo apt-get install -y libnss3 libnspr4 libatk1.0-0 libatk-bridge2.0-0 \
  libcups2 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 \
  libxrandr2 libgbm1 libasound2 libpango-1.0-0 libcairo2
```

#### 5. Загрузка проекта
```bash
# Перейдите в нужную директорию
cd /opt  # или /home/your_user

# Загрузите проект (через git или scp)
# Например:
git clone <your-repo-url> telegram_app
# или
# scp -r telegram_app user@server:/opt/
```

#### 6. Настройка проекта
```bash
cd telegram_app

# Создайте виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установите зависимости
pip install -r requirements.txt

# Установите браузеры для Playwright
playwright install chromium
```

#### 7. Настройка .env файла
```bash
nano .env
```

Добавьте:
```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
ADMIN_IDS=your_telegram_id
API_ROSREESTR_KEY=your_api_key_here
LOG_LEVEL=INFO
API_ROSREESTR_TIMEOUT=120
```

#### 8. Тестовый запуск
```bash
# Запуск через xvfb-run
xvfb-run -a python run.py
```

Если всё работает, нажмите `Ctrl+C` и настройте автозапуск.

#### 9. Настройка автозапуска через systemd

Создайте файл сервиса:
```bash
sudo nano /etc/systemd/system/telegram-bot.service
```

Добавьте содержимое (замените пути на ваши):
```ini
[Unit]
Description=Telegram Bot для Росреестра
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/opt/telegram_app
Environment="DISPLAY=:99"
Environment="PATH=/opt/telegram_app/venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
ExecStartPre=/usr/bin/Xvfb :99 -screen 0 1920x1080x24 -ac +extension GLX +render -noreset
ExecStart=/opt/telegram_app/venv/bin/python run.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

Активируйте и запустите сервис:
```bash
# Перезагрузите systemd
sudo systemctl daemon-reload

# Включите автозапуск
sudo systemctl enable telegram-bot

# Запустите сервис
sudo systemctl start telegram-bot

# Проверьте статус
sudo systemctl status telegram-bot

# Просмотр логов
sudo journalctl -u telegram-bot -f
```

#### 10. Управление сервисом
```bash
# Остановить
sudo systemctl stop telegram-bot

# Запустить
sudo systemctl start telegram-bot

# Перезапустить
sudo systemctl restart telegram-bot

# Просмотр логов
sudo journalctl -u telegram-bot -n 100
```

### Важные замечания

⚠️ **Браузер работает в режиме `headless=False`** - требуется графический интерфейс. На Linux серверах без GUI используйте Xvfb.

⚠️ **Минимальные требования:** 2 ГБ RAM рекомендуется (1 ГБ может быть недостаточно для Playwright + Chrome).

⚠️ **Диск:** Убедитесь, что достаточно места для карт и логов (рекомендуется 20-30 ГБ).

## 📄 Лицензия

Проект для внутреннего использования.
