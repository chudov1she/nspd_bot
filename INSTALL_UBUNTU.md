# 📋 Подробная инструкция по установке на Ubuntu 22.04

Пошаговая инструкция для установки Telegram бота на Ubuntu 22.04 сервер с нуля.

## 📦 Требования

- **ОС:** Ubuntu 22.04 LTS
- **RAM:** 2 ГБ (минимум)
- **HDD:** 20-30 ГБ свободного места
- **vCPU:** 1-2 ядра
- **Доступ:** SSH доступ к серверу

---

## 🚀 Шаг 1: Подключение к серверу

```bash
# Подключитесь к серверу через SSH
ssh username@your-server-ip

# Если первый раз подключаетесь, подтвердите подключение (введите yes)
```

---

## 🔄 Шаг 2: Обновление системы

```bash
# Обновите список пакетов
sudo apt-get update

# Обновите установленные пакеты (может занять несколько минут)
sudo apt-get upgrade -y

# Перезагрузите сервер (если были обновления ядра)
sudo reboot
```

После перезагрузки подключитесь снова:
```bash
ssh username@your-server-ip
```

---

## 🐍 Шаг 3: Установка Python и базовых инструментов

```bash
# Установите Python 3.10, venv, pip и git
sudo apt-get install -y python3.10 python3.10-venv python3-pip git curl

# Проверьте версию Python (должна быть 3.10 или выше)
python3 --version

# Должно вывести: Python 3.10.x
```

---

## 🖥️ Шаг 4: Установка Xvfb (виртуальный дисплей)

Xvfb необходим для работы браузера без графического интерфейса.

```bash
# Установите Xvfb
sudo apt-get install -y xvfb

# Проверьте установку
which xvfb-run
# Должно вывести: /usr/bin/xvfb-run
```

---

## 📚 Шаг 5: Установка зависимостей для Playwright/Chrome

Эти библиотеки необходимы для работы Chromium браузера.

```bash
# Установите все необходимые библиотеки одной командой
sudo apt-get install -y libnss3 libnspr4 libatk1.0-0 libatk-bridge2.0-0 libcups2 libdrm2 libxkbcommon0 libxcomposite1 libxdamage1 libxfixes3 libxrandr2 libgbm1 libasound2 libpango-1.0-0 libcairo2 fonts-liberation libappindicator3-1 xdg-utils
```

---

## 📁 Шаг 6: Создание директории для проекта

```bash
# Создайте директорию для проекта (можно выбрать другое место)
sudo mkdir -p /opt/telegram_app

# Установите права доступа (замените your_user на ваше имя пользователя)
sudo chown -R $USER:$USER /opt/telegram_app

# Перейдите в директорию
cd /opt/telegram_app
```

**Альтернатива:** Если хотите разместить в домашней директории:
```bash
cd ~
mkdir telegram_app
cd telegram_app
```

---

## 📥 Шаг 7: Загрузка проекта

### Вариант A: Через Git (если есть репозиторий)

```bash
# Клонируйте репозиторий
git clone <your-repo-url> .

# Или если репозиторий в другой директории
git clone <your-repo-url> /opt/telegram_app
cd /opt/telegram_app
```

### Вариант B: Через SCP (с локального компьютера)

На вашем локальном компьютере (Windows):
```powershell
# В PowerShell или CMD
scp -r d:\Projects\DmitrySolo\telegram_app\* username@your-server-ip:/opt/telegram_app/
```

На вашем локальном компьютере (Linux/Mac):
```bash
scp -r /path/to/telegram_app/* username@your-server-ip:/opt/telegram_app/
```

### Вариант C: Через SFTP клиент (WinSCP, FileZilla и т.д.)

1. Подключитесь к серверу через SFTP
2. Загрузите все файлы проекта в `/opt/telegram_app/`

---

## 🐍 Шаг 8: Создание виртуального окружения Python

```bash
# Убедитесь, что вы в директории проекта
cd /opt/telegram_app

# Создайте виртуальное окружение
python3 -m venv venv

# Активируйте виртуальное окружение
source venv/bin/activate

# Проверьте, что активировано (в начале строки должно быть (venv))
# Должно быть видно: (venv) username@server:/opt/telegram_app$
```

---

## 📦 Шаг 9: Установка зависимостей Python

```bash
# Убедитесь, что виртуальное окружение активировано
# (должно быть (venv) в начале строки)

# Обновите pip до последней версии
pip install --upgrade pip

# Установите зависимости из requirements.txt
pip install -r requirements.txt

# Это может занять 2-5 минут, подождите завершения
```

---

## 🌐 Шаг 10: Установка браузеров для Playwright

```bash
# Убедитесь, что виртуальное окружение активировано

# Установите Chromium браузер для Playwright
playwright install chromium

# Это может занять 1-2 минуты, подождите завершения
```

---

## ⚙️ Шаг 11: Настройка файла .env

```bash
# Создайте файл .env
nano .env
```

Вставьте следующее содержимое (замените значения на свои):

```env
# Обязательно - токен бота от @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Обязательно - ваш Telegram ID (узнайте у @userinfobot)
ADMIN_IDS=your_telegram_id

# API Росреестра (если есть ключ)
API_ROSREESTR_KEY=your_api_key_here

# Или включите режим симуляции для тестирования
API_SIMULATION_MODE=false

# Опционально
LOG_LEVEL=INFO
API_ROSREESTR_TIMEOUT=120
```

**Как заполнить:**

1. **TELEGRAM_BOT_TOKEN:**
   - Откройте Telegram
   - Найдите бота [@BotFather](https://t.me/BotFather)
   - Отправьте `/newbot` или `/mybots` → выберите бота → `API Token`
   - Скопируйте токен и вставьте в `.env`

2. **ADMIN_IDS:**
   - Откройте Telegram
   - Найдите бота [@userinfobot](https://t.me/userinfobot)
   - Отправьте любое сообщение
   - Скопируйте ваш ID (число) и вставьте в `.env`

3. **API_ROSREESTR_KEY:**
   - Если у вас есть ключ API от api-cloud.ru - вставьте его
   - Если нет - оставьте пустым или установите `API_SIMULATION_MODE=true`

**Сохранение в nano:**
- Нажмите `Ctrl + O` (сохранить)
- Нажмите `Enter` (подтвердить имя файла)
- Нажмите `Ctrl + X` (выйти)

---

## 🧪 Шаг 12: Тестовый запуск

```bash
# Убедитесь, что виртуальное окружение активировано
# (должно быть (venv) в начале строки)

# Запустите бота через xvfb-run
xvfb-run -a python run.py
```

**Что должно произойти:**
- Бот должен запуститься
- В логах должно быть: "🚀 Запуск бота..."
- В логах должно быть: "✅ Бот запущен и готов к работе"
- Бот должен отвечать в Telegram

**Если всё работает:**
- Нажмите `Ctrl + C` для остановки
- Переходите к следующему шагу (настройка автозапуска)

**Если есть ошибки:**
- Проверьте файл `.env` (правильность токена и ID)
- Проверьте логи на наличие ошибок
- Убедитесь, что все зависимости установлены

---

## 🔄 Шаг 13: Настройка автозапуска через systemd

Это позволит боту автоматически запускаться при перезагрузке сервера.

### 13.1. Создание файла сервиса

```bash
# Создайте файл сервиса
sudo nano /etc/systemd/system/telegram-bot.service
```

### 13.2. Содержимое файла сервиса

Вставьте следующее содержимое (⚠️ **ВАЖНО:** замените пути на ваши):

```ini
[Unit]
Description=Telegram Bot для Росреестра
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/opt/telegram_app
Environment="DISPLAY=:99"
Environment="PATH=/opt/telegram_app/venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
ExecStartPre=/usr/bin/Xvfb :99 -screen 0 1920x1080x24 -ac +extension GLX +render -noreset
ExecStart=/opt/telegram_app/venv/bin/python run.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Что нужно заменить:**

1. **`User=your_user`** → замените на ваше имя пользователя
   ```bash
   # Узнайте своё имя пользователя
   whoami
   ```

2. **`WorkingDirectory=/opt/telegram_app`** → если проект в другом месте, укажите правильный путь
   ```bash
   # Узнайте текущий путь
   pwd
   ```

3. **`/opt/telegram_app/venv/bin/python`** → если venv в другом месте, укажите правильный путь
   ```bash
   # Проверьте путь к Python в venv
   which python
   # (должно быть что-то вроде: /opt/telegram_app/venv/bin/python)
   ```

**Пример правильного файла (если проект в `/opt/telegram_app` и пользователь `ubuntu`):**
```ini
[Unit]
Description=Telegram Bot для Росреестра
After=network.target

[Service]
Type=simple
User=ubuntu
WorkingDirectory=/opt/telegram_app
Environment="DISPLAY=:99"
Environment="PATH=/opt/telegram_app/venv/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"
ExecStartPre=/usr/bin/Xvfb :99 -screen 0 1920x1080x24 -ac +extension GLX +render -noreset
ExecStart=/opt/telegram_app/venv/bin/python run.py
Restart=always
RestartSec=10
StandardOutput=journal
StandardError=journal

[Install]
WantedBy=multi-user.target
```

**Сохранение в nano:**
- Нажмите `Ctrl + O` → `Enter` → `Ctrl + X`

---

## 🚀 Шаг 14: Активация и запуск сервиса

```bash
# Перезагрузите systemd (чтобы он увидел новый сервис)
sudo systemctl daemon-reload

# Включите автозапуск при загрузке системы
sudo systemctl enable telegram-bot

# Запустите сервис
sudo systemctl start telegram-bot

# Проверьте статус
sudo systemctl status telegram-bot
```

**Что должно быть в статусе:**
- `Active: active (running)` - сервис работает
- Зелёная точка или `●` - всё в порядке

**Если есть ошибки:**
- Нажмите `q` для выхода из просмотра статуса
- Просмотрите логи: `sudo journalctl -u telegram-bot -n 50`

---

## 📊 Шаг 15: Просмотр логов

```bash
# Просмотр логов в реальном времени
sudo journalctl -u telegram-bot -f

# Просмотр последних 100 строк логов
sudo journalctl -u telegram-bot -n 100

# Просмотр логов за сегодня
sudo journalctl -u telegram-bot --since today

# Просмотр логов с фильтром по уровню (только ошибки)
sudo journalctl -u telegram-bot -p err
```

**Выход из просмотра логов:** Нажмите `Ctrl + C`

---

## 🛠️ Шаг 16: Управление сервисом

### Остановка сервиса
```bash
sudo systemctl stop telegram-bot
```

### Запуск сервиса
```bash
sudo systemctl start telegram-bot
```

### Перезапуск сервиса
```bash
sudo systemctl restart telegram-bot
```

### Проверка статуса
```bash
sudo systemctl status telegram-bot
```

### Отключение автозапуска
```bash
sudo systemctl disable telegram-bot
```

### Включение автозапуска
```bash
sudo systemctl enable telegram-bot
```

---

## ✅ Шаг 17: Проверка работы бота

1. **Откройте Telegram на вашем телефоне/компьютере**

2. **Найдите вашего бота** (по имени, которое вы указали в BotFather)

3. **Отправьте команду `/start`**

4. **Бот должен ответить** и показать главное меню

5. **Попробуйте отправить кадастровый номер** для теста:
   ```
   78:38:0022629:1115
   ```

6. **Проверьте логи** на сервере:
   ```bash
   sudo journalctl -u telegram-bot -f
   ```

---

## 🔧 Решение проблем

### Проблема: Бот не запускается

**Проверьте:**
```bash
# 1. Статус сервиса
sudo systemctl status telegram-bot

# 2. Логи
sudo journalctl -u telegram-bot -n 50

# 3. Проверьте .env файл
cat /opt/telegram_app/.env

# 4. Проверьте права доступа
ls -la /opt/telegram_app/.env
```

**Частые ошибки:**

1. **"TELEGRAM_BOT_TOKEN не найден"**
   - Проверьте файл `.env`
   - Убедитесь, что токен указан правильно
   - Нет лишних пробелов

2. **"Permission denied"**
   - Проверьте права доступа к файлам
   - Убедитесь, что пользователь в systemd правильный

3. **"DISPLAY not set"**
   - Проверьте, что Xvfb запускается в ExecStartPre
   - Проверьте логи: `sudo journalctl -u telegram-bot | grep Xvfb`

### Проблема: Бот не отвечает в Telegram

**Проверьте:**
```bash
# 1. Работает ли сервис
sudo systemctl status telegram-bot

# 2. Есть ли ошибки в логах
sudo journalctl -u telegram-bot -p err

# 3. Правильность токена
# Проверьте токен в .env файле
```

### Проблема: Ошибки при генерации карт

**Проверьте:**
```bash
# 1. Установлен ли Xvfb
which xvfb-run

# 2. Установлены ли зависимости для Playwright
dpkg -l | grep libnss3

# 3. Установлен ли Chromium
ls -la /opt/telegram_app/venv/lib/python*/site-packages/playwright/driver/
```

### Проблема: Недостаточно памяти

**Если сервер с 1 ГБ RAM:**
- Может быть недостаточно для Playwright + Chrome
- Рекомендуется увеличить до 2 ГБ RAM
- Или использовать swap файл (временное решение)

**Создание swap файла (если нужно):**
```bash
# Создайте swap файл 2 ГБ
sudo fallocate -l 2G /swapfile

# Установите права
sudo chmod 600 /swapfile

# Создайте swap
sudo mkswap /swapfile

# Включите swap
sudo swapon /swapfile

# Сделайте постоянным (добавьте в /etc/fstab)
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

---

## 📝 Полезные команды

### Просмотр использования ресурсов
```bash
# Использование памяти
free -h

# Использование диска
df -h

# Использование CPU
top
# Выход: нажмите q
```

### Работа с файлами
```bash
# Просмотр файла
cat /opt/telegram_app/.env

# Редактирование файла
nano /opt/telegram_app/.env

# Поиск в файлах
grep "TELEGRAM_BOT_TOKEN" /opt/telegram_app/.env
```

### Работа с процессами
```bash
# Поиск процесса бота
ps aux | grep python

# Убить процесс (если завис)
sudo pkill -f "python run.py"
```

---

## 🔄 Обновление бота

Если нужно обновить код бота:

```bash
# 1. Остановите сервис
sudo systemctl stop telegram-bot

# 2. Перейдите в директорию проекта
cd /opt/telegram_app

# 3. Если через git - обновите код
git pull

# 4. Если через scp - загрузите новые файлы

# 5. Активируйте виртуальное окружение
source venv/bin/activate

# 6. Обновите зависимости (если requirements.txt изменился)
pip install -r requirements.txt

# 7. Запустите сервис
sudo systemctl start telegram-bot

# 8. Проверьте статус
sudo systemctl status telegram-bot
```

---

## 🎉 Готово!

Бот должен быть установлен и работать. 

**Проверьте:**
- ✅ Бот отвечает в Telegram
- ✅ Сервис работает: `sudo systemctl status telegram-bot`
- ✅ Нет ошибок в логах: `sudo journalctl -u telegram-bot -n 20`

**Дополнительная информация:**
- Логи бота также сохраняются в: `/opt/telegram_app/logs/bot.log`
- Файлы с результатами: `/opt/telegram_app/data/output/`
- База данных: `/opt/telegram_app/bot.db`

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи: `sudo journalctl -u telegram-bot -n 100`
2. Проверьте файл `.env` на правильность настроек
3. Убедитесь, что все зависимости установлены
4. Проверьте использование ресурсов: `free -h` и `df -h`
